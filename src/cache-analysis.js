export function stablePrefixSnapshot({ systemPrompt, tools = [], ralphPrompt, skillText = "", modelSettings = {} } = {}) { return JSON.stringify({ systemPrompt: String(systemPrompt ?? ""), tools: stable(tools), ralphPrompt: String(ralphPrompt ?? ""), skillText: String(skillText ?? ""), modelSettings: stable(modelSettings) }); }
export function longestCommonPrefixBytes(a, b) { const left = Buffer.from(String(a)); const right = Buffer.from(String(b)); const limit = Math.min(left.length, right.length); let i = 0; while (i < limit && left[i] === right[i]) i += 1; return i; }
export function cacheBoundaryReport(prefixes) { const values = prefixes.map((value) => String(value)); const first = values[0] ?? ""; return { count: values.length, byteLengths: values.map((value) => Buffer.byteLength(value)), commonPrefixBytes: values.slice(1).reduce((n, value) => Math.min(n, longestCommonPrefixBytes(first, value)), Buffer.byteLength(first)) }; }
function stable(value) { if (Array.isArray(value)) return value.map(stable); if (!value || typeof value !== "object") return value; return Object.fromEntries(Object.keys(value).sort().map((key) => [key, stable(value[key])])); }

export function cycleSnapshots({ prefix, completions = [] } = {}) { const stable = String(prefix); return completions.map((completion) => `${stable}|${String(completion)}`); }
