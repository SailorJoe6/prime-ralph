/**
 * prime-ralph extension entry point.
 *
 * Slice 1 intentionally installs no workflow behavior. It provides a stable
 * loadable extension boundary for the lifecycle research/POC slices.
 */
export default function primeRalph(_pi) {
  // Deliberately no-op until the research POC validates lifecycle ordering.
}
